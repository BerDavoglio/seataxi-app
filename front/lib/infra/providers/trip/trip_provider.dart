import 'package:flutter/material.dart';

class TripProvider with ChangeNotifier {
  // Create Trip (USER)
  // Index all Trips by OriginID (NAVIGATOR)
  // Update Value (NAVIGATOR)
  // Get Value (USER)
  // Deny Value (USER)
  // Update Accept (USER)
  // Get Accept (ALL)
  // Finish Trip (USER)
  // Index User (USER)
}
